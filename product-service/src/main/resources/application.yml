server:
  port: 8081

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:postgresql://ep-misty-lab-a26wr2tx-pooler.eu-central-1.aws.neon.tech/productDB?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_sC5T9vbWNVLo
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


management:
  endpoints:
    web:
      exposure:
        include: '*'

  endpoint:
    health:
      show-details: always

  info:
    env:
      enabled: true

info:
  app:
    name: product-service
    description: Product Service App built by us
    version: 1.0.0