server:
  port: 8082

spring:
  application:
    name: user-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/userDB
      host: localhost
      port: 27017
      username: tutku
      password: s3cr3t
      database: userDB

management:
  endpoints:
    web:
      exposure:
        include: '*'

  endpoint:
    health:
      show-details: always

  info:
    env:
      enabled: true

info:
  app:
    name: ecom-application
    description: E-commerce App built by us
    version: 1.0.0